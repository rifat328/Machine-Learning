def binary_search(arr, arg):
    while arr == arg:
        quote = 'this thing does exist in the system'
        return quote
    if arr > arg:
        arr


user_input = int(input('Enter how many numbers you want to store? \n: '))
for i in range(0, user_input):
    text_input = input('enter elements'[i])
    print('Task Added to system: ')
    break
print(text_input)
